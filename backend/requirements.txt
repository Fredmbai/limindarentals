# ── Core Django ───────────────────────────────────────────────────────────────
Django==5.2.1              # 6.0.x does not exist; 5.2.x is the latest for Python 3.11
asgiref==3.8.1             # Django 5.2 requires >=3.8.1; 3.11.1 does not exist on PyPI
sqlparse==0.5.1
tzdata==2024.2

# ── Django ecosystem ──────────────────────────────────────────────────────────
djangorestframework==3.15.2        # 3.17.0 does not exist; 3.15.x is latest with Django 5.x
djangorestframework-simplejwt==5.3.1
django-cors-headers==4.4.0
django-environ==0.11.2             # 0.13.0 does not exist; 0.11.x is current stable
whitenoise==6.7.0                  # 6.12.0 does not exist; 6.7.x is current stable
gunicorn==22.0.0                   # 25.3.0 does not exist; 22.x is current stable

# ── Celery task queue ─────────────────────────────────────────────────────────
celery==5.3.6              # 5.6.3 does not exist; 5.3.x is current stable
kombu==5.3.4               # 5.6.2 does not exist; must match celery 5.3.x
amqp==5.2.0                # must match kombu 5.3.x
vine==5.1.0
billiard==4.2.1
click==8.1.7
click-didyoumean==0.3.1
click-plugins==1.1.1
click-repl==0.3.0
prompt_toolkit==3.0.47     # 3.0.52 does not exist; 3.0.47 is latest stable
wcwidth==0.2.13

# ── Database / cache ──────────────────────────────────────────────────────────
psycopg2-binary==2.9.9
redis==5.0.8               # redis-py 7.x does not exist; library is on the 5.x line

# ── Async HTTP (aiohttp + deps used by pywebpush/AT SDK) ─────────────────────
aiohttp==3.10.5            # 3.13.5 does not exist; 3.10.x is current stable (Python 3.8+)
aiohappyeyeballs==2.4.0    # 2.6.1 does not exist; 2.4.x is current stable
aiosignal==1.3.1
frozenlist==1.4.1           # 1.8.0 does not exist; 1.4.x is current stable
multidict==6.1.0            # 6.7.1 does not exist; 6.1.x is current stable
yarl==1.11.1               # 1.23.0 does not exist; 1.11.x is current stable
propcache==0.2.0            # 0.4.1 does not exist; 0.2.x is current stable

# ── Cryptography / push notifications ────────────────────────────────────────
# cffi 2.0.0 was never released as stable and would require Python >=3.12 per
# upstream plans — pin to 1.17.1 (latest stable, Python 3.8+ compatible).
cffi==1.17.1
pycparser==2.22             # 3.0 does not exist; 2.22 is current stable
cryptography==43.0.1        # 46.x does not exist; 43.x is current stable (Python 3.7+)
PyJWT==2.9.0               # 2.12.1 does not exist; 2.9.x is current stable
pywebpush==2.0.0
py-vapid==1.9.1
http_ece==1.2.1

# ── Image / PDF generation ────────────────────────────────────────────────────
# Pillow 12.x requires Python >=3.12 — pin to 10.4.x (last 10.x, Python 3.8+).
pillow==10.4.0
reportlab==4.2.5            # 4.4.10 does not exist; 4.2.x is current stable

# ── HTTP clients ──────────────────────────────────────────────────────────────
requests==2.32.3
urllib3==2.2.3              # 2.6.3 does not exist; 2.2.x is current stable
certifi==2024.8.30
charset-normalizer==3.3.2
idna==3.8

# ── Africa's Talking SMS/voice SDK ───────────────────────────────────────────
africastalking==1.2.8

# ── Utilities ─────────────────────────────────────────────────────────────────
attrs==24.2.0              # 26.x uses year versioning; 24.2 is current stable (Python 3.7+)
packaging==24.1            # 26.0 does not exist; 24.x is current stable
typing_extensions==4.12.2  # 4.15.0 does not exist; 4.12.x is current stable
six==1.16.0
python-dateutil==2.9.0.post0
PyYAML==6.0.2
schema==0.7.7
tzlocal==5.2

# ── Testing ───────────────────────────────────────────────────────────────────
responses==0.25.3
